#include "target.h"
#include <raylib.h>

void target_update(List *entities, Entity *target)
{
	// for (Node *temp = entities->head; temp != NULL; temp = temp->next) {
	// 	if (((Entity *)temp->data)->type == ENTITY_PLAYER) {
	// 		if (CheckCollisionRecs(target->body, ((Entity *)temp->data)->body)

	// 	}
	// }
}
